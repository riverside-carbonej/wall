specVersion: "v1"
serviceId: "wall"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "wall-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
